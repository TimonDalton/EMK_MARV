# Project skills

- **pic-sim** (`.claude/skills/pic-sim/SKILL.md`) — compile a PIC18 `.s` file with `pic-as`, simulate with MPLAB X's `mdb`, inject SCL stimulus, log UART/SFRs. Trigger: `/pic-sim`

When the user types `/pic-sim`, invoke the Skill tool with `skill: "pic-sim"` before doing anything else.
